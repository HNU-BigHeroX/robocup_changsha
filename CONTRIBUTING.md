# 参与指南

参赛代码写在自己的编号目录里，通过同名分支推送到比赛仓库。组织方维护任务环境、评测程序和比赛规则，并在提交截止后统一核验。

还没有配置仓库的话，先看[获取仓库](docs/how_to_fork.md)和[使用指南](docs/how_to_use.md)。下文以 `P017` 为例，请换成组织方分配给你的编号。

## 开始开发

按这个顺序操作：确认编号 → 检查并准备分支 → 创建本人目录 → 开发。

编号分支是 `P017`，本人目录是 `participant/P017/`。如果使用功能分支，要先从编号分支创建并切换过去，再开始该功能的修改；具体命名和命令见 [Git 工作流程](docs/git.md)。

参赛期间只能修改本人编号目录内的提交文件，包括策略、训练脚本、模型、个人配置和报告。请保留官方环境、接口、评测程序、共享文档及其他人的目录。遇到官方代码的问题，记录复现方法并联系组织方。

使用 AI 助手时，先让它读取 [AGENTS.md](AGENTS.md)。助手同样需要遵守目录和分支约定。

## 记录开发过程

每次重要改动都留下一次含义明确的 Git 提交，消息格式见[提交消息](docs/cz.md)。在自己的目录中维护以下材料：

| 文件 | 记录什么 |
| --- | --- |
| `LOG.md` | 这次尝试解决什么问题，做了什么，结果如何 |
| `experiments.csv` | 对应的代码版本、配置、模型和测试成绩 |
| `REPORT.md` | 最终方案、关键对照实验、复现步骤和已知局限 |
| `THIRD_PARTY.md` | 使用了哪些第三方代码或模型，以及来源和许可证 |

模板中的实验记录是示例。请换成自己的实际过程和结果，没做过的实验不要作为自己的结果提交。

开发期间可以向比赛仓库推送本人分支，保留可查阅的迭代历史。统一提交窗口开放前，不得向比赛仓库之外的渠道发布比赛实现。

## 本地测试与报分

在评测环境中先运行预检，再运行公开测试，命令见[使用指南](docs/how_to_use.md#提交预检与公开测试)。缓存、训练中间文件和评分输出留在本地，不要一起提交。

过程成绩按组织方指定的渠道上报，至少注明参赛编号、方案版本、评测版本、公开测试成绩和更新时间。过程排行榜上的分数是参赛者自行测试的结果，最终排名以组织方核验为准。

## 最终提交

统一提交窗口内，从本人编号分支向 `main` 创建 PR。以 `P017` 为例：

- 标题：`[P017] Final Submission`
- 来源分支（head）：`P017`
- 目标分支（base）：`main`

提交前，检查本人目录中的代码、模型、依赖、清单和报告是否齐全，模型摘要是否与实际文件一致，并确认 PR 没有混入本人目录以外的改动。

每人只能有一个有效的最终提交 PR。截止前可以关闭旧 PR 并重新提交；截止后，以最后一个符合规范且在截止前创建的 PR 为准。组织方会在截止时记录提交标识和模型摘要，之后新增提交或替换模型不会改变已接收的版本。详细规则见[赛题与规程](MPE2-协同覆盖选拔赛-赛题与规程.md)第 4 节。

## 报告问题

安装或运行出错时，请附上操作系统、Python 版本、执行命令和完整报错，方便复现。

发现评测漏洞，请通过赛事官方渠道联系评审组，说明受影响的评测环节、触发条件和最小复现步骤。有效漏洞由评审组复核认定；发现和报告漏洞可作为额外能力评价依据，涉及漏洞的性能成绩按修复后的复测结果认定。
